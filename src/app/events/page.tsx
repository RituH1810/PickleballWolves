import Link from "next/link";
import { CalendarDays, ChevronRight, MapPin } from "lucide-react";
import { events } from "@/lib/demo";

export default function EventsPage() {
  return <main className="min-h-screen bg-[#f3f5f2] px-5 py-8 noise sm:px-10"><div className="mx-auto max-w-5xl"><Link href="/" className="text-xs font-bold text-[#6b8f21]">Back to dashboard</Link><div className="mt-8 flex items-end justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#98ba1f]">Open play</p><h1 className="mt-2 text-4xl font-black tracking-[-.04em] text-[#1b211e]">Games worth showing up for.</h1><p className="mt-3 text-sm text-[#67716a]">Browse sessions hosted by your groups and local organizers.</p></div><button className="hidden rounded-full bg-[#1b211e] px-5 py-3 text-sm font-bold text-white sm:block">Create a game</button></div><div className="mt-9 space-y-3">{events.map((event) => <article key={event.id} className="flex flex-col gap-4 rounded-[20px] border border-[#e2e7e2] bg-white p-5 sm:flex-row sm:items-center sm:p-6"><div className="grid h-14 w-14 shrink-0 place-items-center rounded-2xl bg-[#eef2ed] text-center text-xs font-black leading-4 text-[#5c721e]">{event.dateLabel.split(",")[0]}<br />{event.dateLabel.split(" ").slice(-2).join(" ")}</div><div className="min-w-0 flex-1"><h2 className="text-lg font-extrabold tracking-tight text-[#1b211e]">{event.title}</h2><p className="mt-1 text-xs text-[#67716a]">{event.group}</p><div className="mt-3 flex flex-wrap gap-4 text-xs text-[#67716a]"><span className="flex items-center gap-1"><CalendarDays size={14} />{event.timeLabel}</span><span className="flex items-center gap-1"><MapPin size={14} />{event.location}</span></div></div><div className="flex items-center gap-4"><span className="text-xs font-bold text-[#67716a]"><strong className="text-[#1b211e]">{event.spotsLeft}</strong> spots left</span><button className="grid h-10 w-10 place-items-center rounded-full bg-[#d8f24e] text-[#1b211e]" aria-label={`View ${event.title}`}><ChevronRight size={18} /></button></div></article>)}</div></div></main>;
}
